# 2.1 Expected Bronze outputs

The expected Bronze tables are:
- `vattenfall_dev.raw.bronze_market_prices`
- `vattenfall_dev.raw.bronze_weather`
- `vattenfall_dev.raw.bronze_grid_events`
- `vattenfall_dev.raw.bronze_asset_reference`

## 2.2 Validation approach
Bronze validation should confirm that each expected Bronze table exists and contains rows.
The asset reference table is included in the Day 1 validation because it is part of the Bronze foundation and will support later joins with grid event data.


##  2.3 Day 2 Expected Outputs

- Bronze market prices table
- Bronze weather table
- Bronze grid events table
- Bronze asset reference table
- validation SQL results
- documented landing and checkpoint structure

## 2.3 Auto Loader implementation

For the main raw source domains namely (1)  market prices, (2) weather observations, (3) grid events, (4) reference data, in the Bronze ingestion notebooks use Auto Loader with governed Unity Catalog landing paths.

The implemented Auto Loader notebooks include:
- `notebooks/02_bronze/01_market_prices_autoloader`
- `notebooks/02_bronze/02_weather_autoloader`
- `notebooks/02_bronze/03_grid_events_autoloader`

Each Auto Loader notebook defines:
- source domain
- landing path
- checkpoint path
- schema tracking path
- Bronze target table
- ingestion metadata
- validation output

The **reference data** is loaded into the Bronze layer using a simpler Bronze Delta load pattern, as reference files are smaller and less stream-oriented than the 3 main event-style source domains.

Reference Bronze notebook:
- `notebooks/02_bronze/04_reference_data_load`
Target table:
- `vattenfall_dev.raw.bronze_asset_reference


Reference data is loaded into the Bronze layer using a simpler Bronze Delta load pattern, as reference files are smaller and less stream-oriented than the main event-style source domains.

## 2.4 Bronze metadata
The Bronze tables include ingestion metadata for traceability:
- `ingestion_ts`: timestamp showing when the row was loaded into Bronze
- `source_file`: source file path used to trace each row back to the raw file


## 2.5 Bronze validation
The Bronze validation notebook checks:
- table existence
- row counts
- schema visibility
- ingestion metadata presence
- source file traceability
- sample rows for inspection

Validation notebook:
- `notebooks/02_bronze/05_bronze_validation`

The Validated Bronze tables shall include:
- `vattenfall_dev.raw.bronze_market_prices`
- `vattenfall_dev.raw.bronze_weather`
- `vattenfall_dev.raw.bronze_grid_events`
- `vattenfall_dev.raw.bronze_asset_reference`
